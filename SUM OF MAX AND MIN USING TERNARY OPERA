#include<stdio.h>

int main()
{
int a,b,c,d;

printf("enter four numbers :");

scanf("%d%d%d%d",&a,&b,&c,&d);

/* int max=a>b?a:b;

max=max>d?max:c;

max=max>d?max:d;

int min=a<b?a:b;

min=min<d?min:c;

min=min<d?min:d;

*/

int max = a>b && a>c && a>d? a: (b>c && b>d)?b: (c>d)?c:d;

int min = a<b && a<c && a<d? a: (b<c && b<d)?b: (cd)?c:d;

printf("\n sum of max and min is %d", max+min);

return 0;

}
