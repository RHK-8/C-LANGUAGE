#include<stdio.h>
#include<string.h>
int main(){
	char c[10000];
	int count=0;
	printf("Enetr a pars : \n");
	gets(c);
	for( int i=0; i<strlen(c); i++)
	{
		if( c[i]=' ' && c[i+1] !=' '){
			count++;
		}
	}
	printf("\n No.of words in the para are %d",count);
	return 0;
}
